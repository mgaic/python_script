1.重装了系统，舒坦多了。

2.将相关环境配置一下 包括 python selenium chrome webdriver redis mongo

3.将virtualenv中的环境同步过来,让代码能跑起来

4.成功跑起来，今天搞定前程无忧的滑块验证码

5.进入登陆页面,由于其滑块display为none,故打开开发者工具，定位到滑块元素，设置其为块级
	这样就好查看元素，定位元素
	显示滑动框,便于调试 
	document.getElementById("slide_wrapper").style.display = "block";

6.报错call function result missing value 
	谷歌浏览器自动升级导致之前下载的driver无效，尽管可以驱动打开相关界面但是切换frame时报错
	升级chromewebdriver即可解决

7.前程无忧的src并不是一段url，而是base64编码的图片信息，解码得图片

8.反复调试,滑块坐标计算还是有问题,接下来解决这一问题 

9.利用opencv的模糊匹配并不能够准确的匹配到坐标，图片有干扰，故换边缘检测搞定匹配问题

10.使用边缘检测对前程无忧滑块识别率也并不高，而对于腾讯防水墙滑块图背景可直接圈定出缺口图片

11.由于昨天重装系统,下载的chrome为60版本，对应的webdriver也是60版本，而今天谷歌浏览器自动更新，出现了驱动与浏览器版本不匹配问题，随即将webdriver升级为77版本的驱动，但是同样的解决腾讯防水墙滑块的代码，在滑动滑块的使用,并不流畅，停停顿顿，尽管最后能成功的将滑块滑到缺口位置完成验证。将浏览器版本以及driver版本降为60按本，恢复。可见，不同的驱动，同样的代码，在网页上执行的效果可能有所差异，应该是驱动优化的原因
